[buildout]
extends =
    base.cfg

allow-picked-versions = true
show-picked-versions = true

extensions =
    mr.developer

auto-checkout =

[instance]
user = admin:admin
debug-mode = on
verbose-security = on
